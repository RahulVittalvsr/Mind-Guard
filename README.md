# 🧠 MindGuard: Mental Stress Detector

**MindGuard** is an AI-powered application designed to analyze user sentiment and detect signs of mental stress. Built using Python and Natural Language Processing (NLP), it classifies user input into "Stress", "No Stress", or "Unsure" to provide immediate feedback and suggestions.

## 🚀 Features
- **Real-time Sentiment Analysis:** Instantly analyzes text input from the user.
- **Smart Detection:** Uses the Naive Bayes algorithm to classify thoughts.
- **Safety Net Logic:** Includes hard-coded triggers for immediate recognition of critical words (e.g., "happy", "bad", "worse").
- **User-Friendly Interface:** Simple command-line interface (CLI) for easy interaction.

## 🛠️ Tech Stack
- **Language:** Python 3.x
- **Libraries:**
  - `pandas` (Data handling)
  - `scikit-learn` (Machine Learning)
  - `nltk` (Natural Language Processing)
  - `numpy` (Numerical operations)

## 📂 Project  Structure